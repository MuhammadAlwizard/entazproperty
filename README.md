# Deploy branch (generated)

Prebuilt output of the PT Enjaz Instan Properti apps. Do not edit here; the source is on the `main` branch.

- `public/` : public website. Root directory `public`, entry file `server.js`.
- `admin/`  : admin panel. Root directory `admin`, entry file `server.js`.

The host runs `npm install` (dependencies are listed in each package.json). The build command is a no-op.
Environment variables are set in the hosting dashboard, never in this repository.
